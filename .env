# ============================================================
# 🌐 AUTO1 Valinor — Environment Configuration (Committed)
# ============================================================

# --- Global Settings ---
PROJECT_NAME=valinor
VALINOR_NETWORK=valinor_net
POSTGRES_IMAGE=postgres:16

# -------------------------------
# 🗄️ Database Service Credentials (Shared instance settings)
# -------------------------------
# POSTGRES_HOST should match the service name in docker-compose.yml
POSTGRES_HOST=db_main
POSTGRES_USER=valinor_db_admin
POSTGRES_PORT=5432
# POSTGRES_PASSWORD moved to .env.secret!

# -------------------------------
# 🧱 Development Environment (Profile 'dev')
# -------------------------------
DEV_BACKEND_PORT=8700                        # External port for Dev API/Swagger
DEV_FRONTEND_PORT=8701                       # External port for Dev Frontend
APP_ENV_DEV=development
DB_NAME_DEV=auto1_valinor_dev
API_BASE_HOST_DEV=http://api-dev:8000        # Internal Hostname for Scheduler/Workers
ALLOWED_ORIGINS_DEV=http://localhost:5173,http://127.0.0.1:5173,http://localhost:${DEV_FRONTEND_PORT}

# -------------------------------
# 🚀 Production Environment (Profile 'prod')
# -------------------------------
PROD_BACKEND_PORT=8600
PROD_FRONTEND_PORT=8601
APP_ENV_PROD=production
DB_NAME_PROD=auto1_valinor_prod
API_BASE_HOST_PROD=http://api-prod:8000      # Internal Hostname for Scheduler/Workers
ALLOWED_ORIGINS_PROD=http://10.46.0.140:${PROD_FRONTEND_PORT},http://your.custom.domain
# -------------------------------
# 📧 SMTP (Global configuration)
# -------------------------------
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=niklas.skoldborg@auto1.com
# SMTP_PASSWORD moved to .env.secret!